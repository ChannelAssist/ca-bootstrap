# Unattended config for TestSetup_PrereqsDrift_Rejected_ExitsTwo.
# Drift found + user declined to continue → exit 2.
welcome:
  consent: true
prereqs:
  continue_with_drift: false
identity:
  name: "Test User"
  email: "test@example.com"
